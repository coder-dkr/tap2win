<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8">
    <title>Counter Offer Response - {{auctionTitle}}</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            line-height: 1.6;
            color: #333;
            max-width: 600px;
            margin: 0 auto;
            padding: 20px;
        }
        .header {
            background-color: {{#if (eq response "accepted")}}#10b981{{else}}#ef4444{{/if}};
            color: white;
            padding: 20px;
            text-align: center;
            border-radius: 8px 8px 0 0;
        }
        .content {
            background-color: #f8f9fa;
            padding: 30px;
            border-radius: 0 0 8px 8px;
        }
        .response-box {
            background-color: white;
            border: 2px solid {{#if (eq response "accepted")}}#10b981{{else}}#ef4444{{/if}};
            border-radius: 8px;
            padding: 20px;
            margin: 20px 0;
            text-align: center;
        }
        .status {
            font-size: 1.5em;
            font-weight: bold;
            color: {{#if (eq response "accepted")}}#10b981{{else}}#ef4444{{/if}};
            text-transform: uppercase;
        }
        .amount {
            font-size: 2em;
            font-weight: bold;
            color: #667eea;
        }
        .footer {
            margin-top: 30px;
            text-align: center;
            color: #666;
            font-size: 12px;
        }
    </style>
</head>
<body>
    <div class="header">
        <h1>Counter Offer {{#if (eq response "accepted")}}Accepted{{else}}Rejected{{/if}}</h1>
        <p>{{#if (eq response "accepted")}}Congratulations! Your counter offer was accepted.{{else}}Your counter offer was not accepted.{{/if}}</p>
    </div>
    
    <div class="content">
        <h2>Hello {{firstName}},</h2>
        
        <p>{{bidderName}} has {{#if (eq response "accepted")}}accepted{{else}}rejected{{/if}} your counter offer for <strong>{{auctionTitle}}</strong>.</p>
        
        <div class="response-box">
            <h3>Counter Offer Response</h3>
            <div class="status">{{#if (eq response "accepted")}}Accepted{{else}}Rejected{{/if}}</div>
            <div class="amount">${{amount}}</div>
            <p>{{#if (eq response "accepted")}}
                The buyer has agreed to your counter offer price. The deal is now complete!
            {{else}}
                The buyer has declined your counter offer. The auction is now closed.
            {{/if}}</p>
        </div>
        
        {{#if (eq response "accepted")}}
        <p><strong>Next Steps:</strong></p>
        <ul>
            <li>You will receive an invoice shortly</li>
            <li>Please complete the transaction with the buyer</li>
            <li>Contact the buyer to arrange item delivery</li>
        </ul>
        {{else}}
        <p><strong>What happens next:</strong></p>
        <ul>
            <li>The auction is now closed</li>
            <li>No transaction will be processed</li>
            <li>You may relist the item if desired</li>
        </ul>
        {{/if}}
        
        <p>If you have any questions, please contact our support team.</p>
        
        <p>Best regards,<br>The Tap2Win Team</p>
    </div>
    
    <div class="footer">
        <p>This email was sent from Tap2Win Auction Platform</p>
        <p>© 2025 Tap2Win. All rights reserved.</p>
    </div>
</body>
</html>
